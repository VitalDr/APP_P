name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - name: 'Checkout'
        uses: actions/checkout@main
      - name: 'docker login'
        run: docker login -u  17032021 -p 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
      - name: "Build"
        run: docker build -t 17032012/diplom:latest .
      - name: "Push"
        run: docker push 17032012/diplom:latest .
